/**
 * Re-export the shared contract types so app code imports from `@/types`
 * (or directly from `@cafe-pos/types`). Definitions live ONLY in the shared
 * package — never duplicate them here (base prompt §7).
 */
export * from '@cafe-pos/types';
