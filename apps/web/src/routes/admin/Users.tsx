import { Placeholder } from '@/routes/Placeholder';

/** Admin Users / Employees (PRD §8.7 / §13.2). */
export function Users() {
  return (
    <Placeholder title="Users / Employees" prd="§8.7">
      <p>List/add accounts (Name, Email, Username, Role). Per-record: Change Password, Archive, Delete.</p>
    </Placeholder>
  );
}
