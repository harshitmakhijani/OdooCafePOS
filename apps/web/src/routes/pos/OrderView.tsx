import { Placeholder } from '@/routes/Placeholder';

/** POS Order View — product grid, cart + keypad, summary, payment (PRD §9.4). */
export function OrderView() {
  return (
    <Placeholder title="POS — Order View" prd="§9.4">
      <p>Product cards + category tabs + search · cart with Qty/Price/Disc keypad · server-computed summary · Send to Kitchen · payment.</p>
    </Placeholder>
  );
}
